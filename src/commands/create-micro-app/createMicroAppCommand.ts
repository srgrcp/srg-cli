import { Command } from '../Command';
import { ICommand } from 'src/interfaces/iCommand';

export class CreateMicroAppCommand extends Command implements ICommand {
    public Run = () => {
        // TODO
    }
}